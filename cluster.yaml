apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: kuyukiland
  region: ap-southeast-1

nodeGroups:
  - name: ng-1
    instanceType: t3.small
    desiredCapacity: 1
    volumeSize: 20
    ssh:
      allow: false
